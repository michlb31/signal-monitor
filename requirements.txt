feedparser>=6.0.0
requests>=2.28.0
yfinance>=0.2.40
edgartools>=2.0.0
openbb>=4.7.0
pandas>=2.0.0

# Sentiment NLP: il backend lexicon (default) non richiede nulla.
# Per il backend FinBERT (qualità superiore), installare opzionalmente:
#   pip install transformers torch
# e impostare la env var SENTIMENT_BACKEND=finbert
# Sconsigliato su GitHub Actions (torch ~800MB) — il lexicon è sufficiente.
